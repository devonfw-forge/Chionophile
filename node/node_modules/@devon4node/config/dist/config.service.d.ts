import { ConfigModuleOptions } from './config.types';
export declare class ConfigService<T> {
    private readonly configModuleOptions;
    private _values;
    private static configFile;
    constructor(configModuleOptions: ConfigModuleOptions, _values: T);
    static loadConfigFromFile(configDir: string): Promise<any>;
    private loadConfig;
    private removePrefix;
    private validateValues;
    get values(): T;
}
